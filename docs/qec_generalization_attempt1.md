# 第四步首次尝试：先泛化，再扩展纠错协议

2026-09-12 最新用户审批边界：测试脚本、界面交互、序列化和一般工程缺陷可自主修复、记录并重新验收；不再因这类失败请求审批。只有物理模型/物理事实可能出错、需要改动物理硬约束，或整体架构需要调整时才暂停相关方向，给出事实与方案并请求用户批准。不得放宽物理条件、掩盖失败或以改断言冒充通过；保留每次尝试证据。此规则取代此前“任何正式失败都需审批”的规定。 下文旧尝试的统一暂停措辞仅记录当时流程。

2026-09-12。前三步已PASS；本文件在第四步实验开始前声明验收顺序。任何意外失败暂停本阶段及所有依赖扩展，用户批准后才可修改重试。

## 4A 泛化验收

保持既有物理硬条件，允许两块patch的初始原点以显式输入配置；默认原点保持原有(0,0)/(40,0)。新配置必须原样进入初态、平台和可编辑输入，不能只移动动画中的图形。

首次矩阵逐项执行，均使用qec_joint及真实Executor，原SLM完整归还：

1. 一份修改过的短Clifford线路，独立量子预期及效果各一次；明确不是完整GHZ协议，不将protocol false误报成物理失败。
2. 完整GHZ₂输入，seed23、X(Q014)；验证实际syndrome/纠正后码稳定子与逻辑态，不要求与seed7的分支相同。
3. 原481槽Y(Q000)、seed7，patch原点(0,0)/(45,5)，形成横纵错开的二维布局；AOD8×14=112交点，row offsets=0..35步长5，column offsets=0..30及45..75步长5，仍低于既有128容量上限。验证量子、物理、终态与独立重放，不把改变布局的时间与基线作为公平速度比赛。

先静态审查并运行针对新输入的边界/配置验证；再按上述顺序首次编译。每份完整编译墙钟预算600秒，候选/路径/决策沿用4096/100000/10000。意外失败、预算耗尽或量子协议失败立即暂停，不通过增加上限、换输入或换seed重试。保存实际部分trace/checkpoint/recording及失败阶段。有限搜索失败不等于物理无解。

## 4B 通过4A后才实现

两逻辑GHZ：三轮允许噪声的稳定子抽取，加一轮真实完美闭合抽取；闭合轮的运输、测量、复位计入成本。先采用受限单事件模型：三个轮前边界之一的单数据X/Y/Z，或一项前三轮测量报告位翻转；制备、逻辑门、抽取门和RESET保持理想。不声称能纠正任意多个故障或门内hook错误。

实际量子投影和报告位必须区分；条件与译码仅读报告位。采用稀疏历史表可保持显式静态条件电路，包含无错和无需数据纠正的受支持历史；未知历史明确报错，不能借注入故障信息或量子truth选答案。先211个确定性量子案例（162数据事件、48读出事件、1无错）及核心投影/重放/兼容测试，再真实物理代表例、独立重放与可编辑动画。

## 4C 通过4B后才实现

扩展四块二维surface patch，36数据＋32辅助原子；逻辑CNOT树A→B，然后A→C与B→D。必须核验32个码稳定子、全局逻辑XXXX及3条独立相邻逻辑ZZ。AOD容量仍遵守既有128上限，通过合法分组运输解决完整矩形容量，不能忽略空交点。协议、量子验证、物理编译、独立重放及编辑动画均须单独通过，才将整个第四步标为完成。

本阶段结果均受声明的门集、几何、预算与噪声模型限制，不是全布局最优或完整电路级容错证明。
