# 4B 多轮测量纠错验收结果

2026-09-12，PASS。可编辑结果： http://127.0.0.1:8786/?job=62f802cad9766946821a0523418c45f3 。这是完成并保存的实际物理执行；打开页面不会隐式重新编译。修改门表后点击编译会重新运行物理编译器。

## 已完成的内容

两个旋转d3 surface logical qubit，18数据原子、16稳定子辅助原子。制备逻辑GHZ后执行三轮带报告噪声的稳定子测量，再执行一轮理想闭合测量，最后按报告历史做条件Pauli纠正。测量、复位、运输和纠正都有实际Executor事件。报告翻转不会伪造量子投影，decoder只能读取报告位。

协议使用211个声明的单事件案例验证：162个轮边界单数据Pauli、48个单测量报告翻转和无故障案例。222项协议测试全部通过；底层57项、接口与调度26项也通过。这些有不同范围，不混称全仓库测试。

## 实际物理代表

seed7，第二轮A块X0测量真值0、报告1。编译输入916门/控制槽，249CZ、80MEASURE、80RESET，全部效果各提交一次。decoder识别报告错误，最后无需数据纠正；制备阶段真实执行Z(Q011)。16个码稳定子及逻辑XX/ZZ最终全部+1，所有34原子恢复原SLM、坐标及开关条件。

| 指标 | 实际值 |
| --- | ---: |
| 完整物理时间 | 46463.3 μs |
| 逻辑完成时间 | 46073.3 μs |
| 最后归还/恢复 | 390 μs |
| LOAD / OFFLOAD | 88 / 88 |
| CZ脉冲次数 | 81 |
| 测量 / 复位批次数 | 20 / 20 |
| 测量 / 复位设备忙时 | 10000 / 2000 μs |
| Raman设备忙时 | 72 μs |
| 单次编译墙钟 | 1235.164 s |
| 独立重放墙钟 | 641.370 s |

256个序列化计划在新初态由Executor独立重放，未调用编译器；终态checkpoint逐字相同，读出历史及量子状态再次通过。指标是本机单次观测；4B较旧单轮电路增加工作，不能直接拿总时间声称调度加速或退化。

## 界面验收

完整执行在物理和关键帧模式分别以32×播放到底，用时59.187和53.25秒；原子精确返回原holder和坐标。噪声测量中点未显示未提交结果，完成后才显示真值与报告位。

另实际编辑并编译六门线路：H(Q000)、CZ(Q000,Q009)、H(Q009)、CZ(Q000,Q009)、MEASURE(Q018,报告翻转)、RESET(Q018)。2586.6μs完成，6个效果各一次，真0/报告1、复位后Z=+1，原SLM终态通过。该短线路的物理完成与完整GHZ协议未成立分别报告。无页面异常，正式执行期间源码未变。

证据：`artifacts/qec-roadmap/step4B-attempt1/verification.json`、`step4b-ui-attempt1/browser-acceptance.json`；可复现脚本`examples/run_qec_temporal_acceptance.py`与`examples/accept_temporal_qec_ui.py`。

## 适用边界

单比特1μs、最近原子距离至少5μm、同类门批次，保持既有物理规则。测量500μs、复位100μs是模型参数，不是设备普适实测值。报告噪声是明确单事件模型，闭合轮假设理想但其物理时间完整计入；没有验证全电路噪声容错、阈值或任意多错误纠正。4C四逻辑扩展另行验收。
