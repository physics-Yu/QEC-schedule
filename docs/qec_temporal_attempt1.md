# 4B：时域读出纠错的首个物理验收

4A已完成三输入独立物理重放和真实可编辑UI验收。按预声明路线实现4B，不改变运输、支撑、全EZ作用对、Raman间距及时间规则。

范围是两个d3逻辑patch：完美制备、GHZ、三个允许单事件的抽取轮、一个真实完美闭合轮。共80次目标测量、80次RESET、249个CZ；它们的运输与设备时间均计入。保证仅限一次轮前数据Pauli测试脉冲，或一次测量报告位翻转，不是全电路噪声容错模型。

先通过211个量子协议案例和新增核心/兼容/工作台检查，再进行一份完整物理代表：seed7，第二轮patch0的X0读出报告翻转。量子投影不翻转，译码只接收64位reported时域历史；无需数据纠正也必须命中显式支持集合。这个例子检验测量错误不会诱发错误的数据纠正。数据错误的恢复先由162个量子协议案例验证，不宣称它们各自已做全几何编译。

物理代表保持原点(0,0)/(40,0)、7×14非均匀98交点AOD和原SLM全归还。单次编译和独立重放分别预算1800秒：当前五次完整抽取对比原两次，且新增64位历史条件；不通过增加作用距离、忽略空交点或改变闭合成本完成。候选4096、路径节点100000、决策10000沿用已验证参数。保存每40门的实际checkpoint与progress，预算耗尽不能声称物理无解。

编译结束要求：所有效果各一次、80reported结果、恰一个实际true/report差异、history_supported、协议完整、16码稳定子与逻辑XX/ZZ正确、34原子活着并未测量、全原SLM终态、checkpoint恢复。随后compiler-free逐plan重放要求最终checkpoint逐字一致，并再次核验量子结果。最后使用新一致代码服务恢复完整结果，支持任意Clifford门编辑和MEASURE报告翻转开关，完成真实短线路编译及双模式32×回放。

工程问题按用户最新授权自主修复并保留每次证据；物理模型可能错误、需改物理约束或整体架构时暂停并请求审批。4C在4B整体通过后才开始。
