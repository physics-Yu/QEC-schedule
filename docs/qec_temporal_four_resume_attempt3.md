# 4C 从已提交检查点继续验收

2026-09-12。第二次完整尝试在3600秒工程预算结束，最后进度1604/1868、7568事件；可恢复的完整检查点为1588槽、7564事件、94372.5μs。没有报告物理约束错误，原超时与产物保留，不把超时标为PASS。

用户已明确：普通工程修复自行完成；物理事实/约束或整体架构需要改变时才审批。本次增加显式完整计划边界续编译入口，仍使用同一DAG、量子状态、测量报告、RNG、物理后端、候选顺序和Executor。核对原input与原始完整terminal，由stage记录重建retained cohort，拒绝部分操作、错误支撑和未知综合征历史。旧默认入口不变。

77项专项通过（20.96秒），包含五种切点与不中断执行的完整snapshot及后续计划逐字比较、非法边界拒绝、128位历史启动guard、8193条连续缓存扫描及旧读出/快照/joint回归。

解析缓存16384条；编码缓存1536MiB、按需分配。旧1588前的1496槽checkpoint已494MB；原768MiB同时计算原文与转义编码，后段可能超出保留容量。旧运行最终事件仍小于8192，不能说已触发解析缓存循环失效；超过8192是已完成轮次推算的后续风险。新运行记录真实缓存保留量和进程内存，最终要求完整记录被编码缓存保留。

脚本 `examples/resume_qec_temporal_four_acceptance.py`，输出 `artifacts/qec-roadmap/step4C-resumed-attempt3`。后缀编译和全轨迹独立重放各3600秒；全局10000计划扣除已提交前缀，局部4096候选/100000路径预算、输入、物理参数不变。保存前缀SHA、原尝试计时、源码差异和本次源码冻结；不得把后缀时间称完整编译时间。

独立重放从原初态执行全部前缀和后缀，每个事件逐字匹配，并重新录制完整动画。其余物理、逻辑、历史、归还和可编辑界面通过条件沿用首次合同。尚未执行成功，不能标4C/第四步完成。
