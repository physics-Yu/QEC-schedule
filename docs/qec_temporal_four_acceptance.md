# 四逻辑 surface GHZ 时域纠错验收

2026-09-12。最终：完整物理执行、全轨迹独立重放及可编辑界面全部PASS；第四步及四步路线按声明合同完成。实时状态见 `instruction/handoff.md` 与 `artifacts/qec-roadmap/status.json`。

## 线路与参数

四个 rotated [[9,1,3]] patch，36个数据原子、32个稳定子辅助原子；先制备码态，再执行逻辑GHZ树 A→B、A→C/B→D，进行三轮带声明噪声的稳定子抽取和一次实际执行的完美闭合轮。纠正只读取报告历史，不读取故障标签或真实投影结果。

| 项目 | 本次值 |
| --- | --- |
| 原子与完整线路 | 68原子，1868个门/条件控制槽 |
| 固定轴AOD | 7×14=98个交点，所有活动空交点也纳入校验 |
| 四块原点 | (0,0)、(40,0)、(0,40)、(40,40) μm |
| CZ/MEASURE/RESET | 507/160/160个效果 |
| 1Q时间 | 1μs，同类型且满足物理条件时并行 |
| MEASURE/RESET时间 | 500/100μs，显式仿真假设 |
| 综合征 | 128位纠正历史，160次总测量 |
| 代表噪声 | seed7，第二轮patch A的X0报告翻转 |
| 路径与作用校验 | 原半格正交通道、捕获/支撑/全段避碰、整EZ实际CZ对校验 |
| EZ近邻格保护 | 本surface输入显式关闭，沿用用户授权 |

两时域profile与普通线路使用同一编辑→物理编译→Executor→recorder→viewer流程。QEC模式可编辑H/X/Y/Z/CZ/MEASURE/RESET；普通模式支持T，当前Clifford量子模型明确拒绝QEC模式的T。修改后的短线路可物理执行完成而不满足完整GHZ协议，两种状态分别报告。

## 物理执行结果

1868个门/条件控制槽全部按DAG完成一次，515个已提交计划、8360条事件。包含归还的仿真总时长105285.6μs，其中逻辑完成104115.6μs、最终恢复1170μs。装载/卸载各231次，所有68原子返回原SLM、坐标及开关/AOD终态。

实际最大H批次52、CZ批次18；160次MEASURE与160次RESET各为20个8原子批次。CZ批次分布为2对×140、4对×50、9对×1、18对×1；这里是已执行记录，不是纯候选或预设动画。条件为假的X/Z控制槽有逻辑效果记录但不打光，不得用其数量冒充Raman物理并行。

32个码稳定子、逻辑XXXX、AB/BC/CD三条逻辑ZZ均为+1。128位报告历史受声明decoder支持；唯一差异为round2_X0_0：真实投影0、报告1，其余159次一致。该报告历史被受限decoder接受并产生空数据纠正；制备阶段另有5个实际条件纠正，352个X/Z条件槽中其余347槽不打光。所有辅助原子已真实测量并复位。

## 工程失败与恢复来源

第一次完整尝试因已证实的旧缓存容量风险受控停止。第二次在3600秒预算结束，最后进度1604槽，保留的完整检查点为1588槽/468计划/7564事件。没有报告物理约束错误。旧失败记录未删除或改成通过。

普通工程问题按用户最新长期授权自主修复。显式续编译入口核对原输入、完整计划边界、原始终态及retained cohort；旧默认路径不变。77项专项检查通过，包括五种切点的完整snapshot与后续计划逐字一致，以及非法恢复和未知历史拒绝。

从原检查点恢复的canonical SHA256为 `51d51d321d600940c64642e2f53fdbab5c7acda17b027690cea9609425f36674`，与原文件一致。只继续后缀47计划，耗时631.276秒；该数值不是完整编译时间。原前缀保存时3579.865秒，两段合计约4211秒，另外的失败尝试与丢弃尾部另保留。编译速度仍需改进，缓存容量修复没有消除完整状态哈希随历史长度增长的成本。

解析缓存上限16384条，编码缓存上限1536MiB，均按需保留。本次最终完整编码缓存1053951112字节，记录全部8360条；续编译进程观测峰值4176789504字节。它们是工程工作集参数，不是物理AOD或搜索空间的变化。

## 验证层次与边界

- 四块量子协议437项通过，包含421个声明单事件与独立综合征/观测量检查。
- 68原子分组stage/18CZ/完整restore及独立重放的几何代表已通过；接口与旧两块回归通过。
- 完整代表的物理终态、报告/投影、门效果与恢复检查通过；原初态515计划逐事件独立重放PASS，8360事件与最终checkpoint逐字一致，耗时2393.463秒；完整前缀/后缀动画由该重放重新生成。
- 可编辑界面一次验收PASS：物理32×132.062秒、关键帧32×124.484秒全程归还68原子，测量无预读。另六门实际编辑编译4006.6μs，真0/报告1、Q036复位Z=+1，六项各完成一次，68原位归还；短线路非GHZ/协议不完整为预先声明预期。浏览器无page errors，UI期间源码稳定。

这是仿真中的受限单事件恢复：三噪声轮至多一个数据Pauli或报告错误，加完美闭合轮。没有宣称全电路噪声容错、阈值、设备保真度或任意布局全局最优。完整硬件时长与串扰模型仍需在对应设备数据下另行标定。

原始证据位于 `artifacts/qec-roadmap/step4C-resumed-attempt3/`，包含父尝试来源、输入、全trace、checkpoint、物理结果和缓存记录；完整recording与HTML由独立重放从初态重新生成。


## 可编辑交付与复现

[打开完整四逻辑工作台](http://127.0.0.1:8787/?job=87eef4dc58e467e0e6b423bfda158c43)。新服务8787/PID28532；旧8786两块结果等均保留。完整物理重放后仅修改workbench_server.py恢复元数据与workbench.js时间/日志口径显示：后缀631.28秒、后缀47服务，前缀1588槽/468计划及原观察耗时另列，动画完整包含前后缀。19项相关回归及3个Node控件检查通过；物理模块未修改，源码差异在`step4c-ui-attempt1/post-replay-ui-source-delta.json`记录。

正式UI证据`artifacts/qec-roadmap/step4c-ui-attempt1/browser-acceptance.json`；实际六门编译产物在同目录`short-compile/`。UI结束后未再启动编译。恢复服务命令：

```powershell
C:/python312/python.exe examples/circuit_workbench.py --port 8787 --output artifacts/workbench-qec-temporal-four --restore-job-dir artifacts/qec-roadmap/step4C-resumed-attempt3
```
