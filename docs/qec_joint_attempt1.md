# 第三步首次尝试：批光与读出运输联合选择

开始前合同，2026-09-12。步骤2已完整PASS，本attempt不得在失败后自行修改重试。

## 声明方案及预算

1. 核心提供一个真实RAMAN_ROTATION的同型、不同目标gate_ids批操作。仍固定1μs，只有一个共享AOD运输锁；逐目标验证稳定支撑、距离、门类型与真实条件，禁止用资源放宽允许载原子移动中打光。混合真假条件只给实际执行目标打光、计入忙时。
2. 新独立策略`qec_joint`保持步骤2的CZ候选及MEASURE/RESET顺序；同类READY单比特门使用上述批操作。原策略行为保留。
3. 每次读出访问最多比较两类服务：已验证的原SLM落地服务，和保留相同原子在MZ静止AOD上MEASURE→RESET→直接返回的服务。相同起终态、门效果与随机投影顺序下按完整物理时间选较短合法候选。不能删除装卸却保留不真实的交接豁免；开启SLM重叠等仍需完整校验。
4. 继承CZ候选上限4096、路径节点上限100000、决策上限10000。读出保持原组顺序；本轮不通过重排测量、换seed、加搜索上限来制造收益。记录候选成本、拒绝原因与墙钟，内程序一致性错误必须上报。

这是一轮有界批次/读出/运输联合优化，不声称已实现全局CZ重排最优或动态AOD轴规划。

## 首次验收次序

- 静态审查后，先运行核心同型批次测试：多个SLM/AOD合法并行、混合真假条件正确且无假光、全部false零忙时、异型/共享原子/小于5μm/移动或未稳定支撑作为预先声明负例拒绝，逐事件恢复和实际效果一次。
- 小规模真实MZ保留读出与原服务对照，要求同分支/同终态且省去一对装卸；开启SLM冲突的候选应被正常排除并选用声明合法后备，不将内部错误当后备。
- 冻结源码后，只运行一次原481槽完整输入：物理时间严格低于步骤2的20885.9μs、LOAD少于45、完整32测量位/32RESET投影/实际纠正与冻结基线一致，量子协议及终态通过。编译墙钟报告，不把单次时长改善当统计加速证明。
- 独立compiler-free物理重放与checkpoint一致；随后可编辑策略入口和完整双模式32×动画验收。

任何意外失败按主合同暂停并报告，步骤4保持未开始。以上列明的负例和单个几何候选拒绝不是阶段失败；有限预算用尽、收益不成立、内部不一致或实际浏览器验收失败则是正式失败。
