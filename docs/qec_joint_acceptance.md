# 第三步：同型批光与MZ保留读出

2026-09-12 attempt1：PASS。74项首次专项测试、完整481槽量子/物理数值检查、独立120计划物理重放PASS（151.323秒，481效果各一次，checkpoint精确一致，纠正前XX/ZZ为−1/−1、后+1/+1）；真实工作台验收也已通过，持久状态见`artifacts/qec-roadmap/status.json`。

| 同输入及原SLM归还终态 | 原基线 | 步骤2持续复用 | 步骤3联合选择 |
| --- | ---: | ---: | ---: |
| 完整物理时间（μs） | 22055.9 | 20885.9 | 19255.9 |
| 装载 / 卸载次数 | 51 / 51 | 45 / 45 | 37 / 37 |
| Raman忙时（μs） | 33 | 63 | 33 |
| CZ个数 / pulse数 | 105 / 33 | 105 / 33 | 105 / 33 |
| AOD路程（μm） | 3498 | 3498 | 3498 |
| 计划数 | 93 | 150 | 120 |
| 本机单次编译墙钟（秒） | 189.438 | 326.401 | 206.696 |

八次MZ访问均选择了合法的保留AOD读出服务，各省200μs，合计1600μs；真批量Raman恢复多移动原子的同时打光，消除步骤2多出的30μs。因而比步骤2再省1630μs，相对原基线总共省2800μs（12.70%）。原子总路程15610μm、测量/复位忙时4000/800μs保持。CZ并行规模和路程没有改善，不能把本结果描述为CZ排程最优。

这是两类服务的有界比较：原SLM服务仍按原策略选择第一个合法目的地；额外保留服务只尝试该同一目的地一次。比较前核对预测placement、AOD、SLM、atom、量子态、DAG、测量结果与RNG终态一致，以及MEASURE/RESET精确效果顺序一致。新的reference builder逐目的地直接传播内部一致性错误；正常几何候选才可继续。

原生Raman批次是单个1μs operation，gate_ids必须同型且目标不同；一次AOD锁稳定支撑。每个真实目标分别校验；混合条件逐目标打光，全部false时零Raman忙时。历史AOD_busy统计仍统一排除Raman，避免三策略口径变化；它并非所有AOD资源锁占用的区间并集。

完整运行验证481效果各一次、32测量位/32RESET投影/3个实际条件纠正与冻结基线一致，全部原子归还、量子GHZ₂与协议完整检查通过，源码在正式运行期间未变。候选排除仍仅64个PATCH_CAPTURE_CLOSURE与4个CAPTURE_CHANGED；没有内部错误后备掩盖。

证据：`artifacts/qec-roadmap/step3-attempt1-tests/pytest.txt`、`artifacts/qec-roadmap/step3-attempt1/`。编译墙钟比步骤2改善，但仍略慢于原基线；单次观测和不同验证工作量不足以作为统计加速比。

真实浏览器检查4个移动原子同步H、MZ中4个移动原子MEASURE/RESET（无预读）、全部3个真假混合条件批只给各1个真实目标画光；两模式32×分别24.625/23.578秒完整到原34个SLM终态，无page error。真实编辑6门另编译成功，1247.6μs、效果各一次，预先声明非GHZ/协议false符合预期。运行源码未变。

可编辑完整结果：`http://127.0.0.1:8784/?job=03a43acfc3a55199bc9ce6d971d37a92`，PID28316。完整481结果仅恢复保存的真实执行，没有重复编译。证据在`step3-attempt1/ui-subcheck/`。

本轮不等于全局门重排、动态AOD伸缩、重复带噪纠错或四逻辑GHZ已完成。后续步骤4仍须单独验收。
