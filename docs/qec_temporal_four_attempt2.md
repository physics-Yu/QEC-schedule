# 4C 第二次完整尝试：工程缓存修复

2026-09-12。首次在744/1868、899.3秒处受控停止；最近完整保存点740槽、881.19秒。没有观察到物理失败，也未耗尽3600秒预算。原输入、进度、checkpoint及被停止进程的退出记录保留在`step4C-attempt1`。

复制检查点上的实测证明顺序扫描工作集大于LRU容量时会循环淘汰：1784记录、4096缓存热扫1784命中；缩放为512容量再扫同样记录，零命中、1784次重新解析，约1秒。当前744槽已有约2700事件，后续轨迹预计超过旧4096上限。另JSON编码前缀缓存256MiB已不能容纳增长的历史。

第二次仅调整内部缓存：解析事件8192条；编码缓存768MiB上限，不预分配、不改变checkpoint内容。本机16GiB，首次运行worker约0.74GiB时仍约4.7GiB可用。缓存优化先单独验证；这不是永久解决任意长度轨迹的方法，也不把缓存空间当成AOD容量或搜索预算。

执行契约沿用`qec_temporal_four_attempt1.md`：同1868输入、seed7、报告错误、68原子、98AOD、物理规则和局部搜索预算，编译与重放各3600秒。新输出`artifacts/qec-roadmap/step4C-attempt2`；新脚本启动时立即保存源码指纹，进度增加trace记录数。结束必须证实该代表总记录数小于8192，完整物理、独立重放、UI条件保持不变。完成后另核对首次保存的740槽trace是新完整trace的逐字前缀，验证缓存变化未改变已有物理过程。

工程失败依据用户最新长期授权自行修复并记录；只有物理模型疑点、硬约束或整体架构改动才需要请求审批。
