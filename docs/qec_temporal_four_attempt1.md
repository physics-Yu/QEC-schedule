# 4C 完整物理与界面验收合同

2026-09-12，执行前声明。4A、4B已通过，4C尚未通过。

## 前置检查

- 421个声明范围内的单事件量子案例及协议负向检查通过。
- 默认四块布局的68原子分批stage/restore通过普通物理后端；跨块9/18对CZ的候选终点与运动按实际后端检查。
- 新工作台profile、四原点、读出翻转、历史guard及旧两块回归通过。
- 状态编码缓存已由旧基线及132.9MB的4B检查点逐字和SHA256对照验证，不减少审计字段。

## 首次完整代表

`surface_qec_temporal_four.experiment_input`，seed7，第二噪声轮patch A的X0报告翻转。68原子（36data、32ancilla），1868个门和控制槽，160MEASURE、160RESET、507CZ。四原点(0,0)/(40,0)/(0,40)/(40,40)；7×14=98固定非均匀轴AOD，捕获闭包和空活动交点全部检查。

使用已有joint门排序与新高度分组运输；实际每个CZ批次由编译器和全EZ作用对验证决定，9/18是候选能力，不能事先冒充完整运行成绩。单比特1μs、MEASURE500μs、RESET100μs保持；后二者是仿真假设。三噪声轮加一个实际执行的理想闭合轮，受限单事件历史decoder不称全电路噪声容错。

## 资源与失败记录

代码`examples/run_qec_temporal_four_acceptance.py`，产物`artifacts/qec-roadmap/step4C-attempt1/`。编译与独立重放分别最多3600秒，按较4B约两倍电路/原子规模预设；不是扩大局部贪心搜索。决策10000、候选4096、路径100000保持。优先应用已验收的等价编码优化，无隐式提高容量或跳过validation。

源码冻结；保存输入、策略、进度、定期完整最近提交checkpoint，结束保存全部trace/recording/decisions/diagnostics。每20个重放计划写独立进度，不将工作线程输出延后到全部结束。脚本一次执行、不自动重复正式实验；工程失败保存证据后可按最新用户授权修复再验，物理错误可能性/硬条件或框架改动才需请用户批准。

## 通过条件

全1868效果exactly once；160位真实报告提交，唯一指定报告翻转；128位历史完整且受声明decoder支持；32码稳定子、逻辑XXXX与AB/BC/CD三条ZZ全部+1。所有68原子返回原SLM/坐标，全部开关、AOD配置、量子测量状态满足终态。

全部已提交计划在新初态由Executor独立重放，不调用compiler；最终checkpoint逐字一致，再验证历史与逻辑观测量。完整物理及重放成功后才启动8787，恢复结果、双模式32×完整播放、测量无预读、六门含翻转/RESET的真实编辑编译。短编辑线路完成物理执行但不满足完整GHZ协议时明确显示两种状态。上述检查全通过方可将4C及第四步标为PASS。
