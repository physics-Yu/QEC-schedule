# 2026-09-10 · Milestone 2 连续 eager 调度

- 状态：COMPLETED（本轮 rigid M2 范围）。
- 用户目标：按照 agent/handoff 完成 M2；先修复 BUG-001/002，再实现多计划、指标、换伙伴、恢复和报告。
- 起点：M0/M1，80 项历史测试；最终 M0–M2 回归为 111 项。
- 相关规范：agent、handoff、model_audit、motion_execution、milestones、physics、validation、workflow、visualization。

## 完成内容

1. `simulation/executor.py` 拒绝 schedule/step 的 GATE_* 注入；`testing/logical_executor.py` 显式隔离 M0 逻辑演示。失败不消费事件、不改变状态；测试场景及跨进程逻辑恢复切换到专用入口。
2. `simulation/runtime_validation.py` 定义 committed event boundary 校验，在 restore、step 前、完整下一状态提交前及 run 检查。核对 plan/trace、cursor、预约、唯一下一事件、时长、placement/pose、移动标志及 pulse/DAG；缺失 LOAD completion 不再静默结束。
3. `domain/operations.py`、`simulation/state.py`、`physical_executor.py` 定义 episode 起点、总 wall、last pulse、完整逻辑完成（未完成为 null）、last cycle、plan count、每计划 trace duration、利用率/吞吐量。空载只计 AOD 路程；兼容字段原有语义保留。schema 5 与相对 interaction_offset 明确拒绝旧快照。
4. `planning/eager_baseline.py` 遍历完整稳定 READY frontier，选择首个可编译计划；`simulation/scheduler.py` 连续执行，返回 completed/stalled，损坏 runtime 抛错。逻辑后继在 pulse 完成释放，下一计划等待 cleanup。
5. `motion/compiler.py` 支持显式空载定位与归还、按实际伙伴及目标 cell 求作用 pose；沿既定折线执行，完整捕获、整段 clearance、全 EZ pair 均保留。只允许 CZ 物理执行，避免缺少 θ 的 CPHASE 被当作 CZ。M0/M1/M2 factory 的 grid 修正为真实坐标。
6. `milestone2_factory.py` 与 `testing/milestone2_report.py`、`examples/run_circuit.py` 覆盖重复 pair、真实换伙伴、三门 ab/ac/bd、汇合 ab/dc/ac、unsupported。共享 animation/timeline/replay 去除固定 G000/九段假设；每帧显示当前门与实际 frontier，每个 operation 带正确 capture 集。空载提示明确原子保持 SLM。
7. 更新 agent、README、相关 instruction、M1/M2 docs、handoff 和日志索引；未改历史日志或归档原件。

## 本轮验证

| 命令/检查 | 结果 | 证据 |
| --- | --- | --- |
| `python -m pytest --visual -q` | 111 passed，1 个既有 dateutil 弃用警告 | M0 六组报告重建；包含 M1/M2 全部机器测试 |
| `python -m pytest tests/test_milestone0.py tests/test_foundations.py tests/test_milestone1.py tests/test_milestone2.py tests/test_report_workflow.py -q` | 并行字段扩展出现后再次 111 passed；40.86 s | 针对 M0–M2 的最后一次机器回归 |
| `python examples/run_single_gate.py` | 5 个 M1 验收场景 PASS | `artifacts/milestone1/index.html`；312.3/156.3 μs、56 μm 不变 |
| `python examples/run_circuit.py` | 5 个 M2 验收场景 PASS | `artifacts/milestone2/index.html`，plans/metrics/trace/snapshots/diagnostics |
| `node tests/replay_controls.cjs` | baseline/incidental/three_gate/join PASS | 含多计划阶段、空载提示、真实 capture/pair、frontier、双时钟、不变性 |
| 恢复 | 三门全部 65 个 snapshot 边界 + pending PLAN_STARTED 与完整执行一致 | `test_resume_every_boundary_including_second_plan_and_empty_moves` |
| 损坏恢复 | 全部活动边界上 12 类损坏拒绝 | 缺失/重复/错属/错序/错时/游标/开始时刻/预约/plan/移动标志/pose/丢 runtime |
| 静态查看 | 第三门关键帧与完整 29 段时间线通过 | `three_gate/frame_52.png`、`timeline.png` |
| 实际浏览器烟雾检查 | three_gate 第三门按钮、2 μm 配对及侧栏；最后空载段与 final 通过 | 888.60 μs Q001–Q003；1072.90 μs 全部 SLM；1112.90 μs、v64、下一事件禁用 |
| 文档 | 当前规范/README/docs 本地文件链接无缺失 | 不重写/遍历归档正文 |

三门独立数值：wall=1112.9 μs、逻辑完成=888.9 μs、AOD/atom 路程=256/216 μm、3 plans、29 operations、64 events。加入第三计划附带 Q004=(25,5) 时 atom 路程=300 μm，AOD 仍为 256 μm。详细逐段算式见 [M2 说明](../../docs/milestone2.md)。

过程中一个新增测试最初把附带原子放在 storage 范围外的 (30,5)，由 INVALID_STATIC_SITE 正确拒绝；修正测试输入到已有合法站点 (25,5)，未扩大 zone 或放宽物理参数。join 的独立核算按 dc 的 32 μm 单程纠正，未据输出直接改模型。

## 并行工作与交付边界

末尾检测到同目录另一活动任务新增 `domain/aod.py`、row_column backend、AOD offsets 与硬件 acceleration/jerk 等字段。本轮没有覆盖或撤销这些修改，也未将它们列为 M2 完成能力。旧产物因新增字段的规范化检查失败后，已通过 M1/M2 生成入口重新生成，并再次通过上述 111 项 rigid 路径测试和 Node 检查。

最后检查期间 10 个关键源文件指纹未变；记录在 `artifacts/milestone2/source-fingerprints.json`。本轮编译器 SHA-256 `a415aa1de1380aabafd90fb480e9cbd890296a74347fd30cfec3ceebabb01f17`；scheduler `553225d473aad24a12750f757868670e2701d3c89628d7b9f29848ad32a1acca`；runtime validator `d9e9ac2f5116d0f18bfcccf4a36214794900fd491a862510d0c3e480061f8ef9`。另一任务此后继续更改底层/schema 时，需要重新验收，不能沿用本轮 PASS 声明其新 backend 正确。

## 决策与限制

- M2 完成限定在预置静态 EZ 伙伴 + 区外静态源 + 可行固定折线路由；测试布局的 sources 在 storage。不含全 storage 的伙伴准备，也不是通用路由器。
- 单 AOD、单 gate、RETURN_AND_OFFLOAD；所有 resources 整个 plan 预约，不代表激光全程开启。
- 参数仍为未标定运动学模型。未实现 KEEP、batch、多 AOD、任意 θ 门、物理测量结果、量子态、真实 fidelity 或 RL。
- committed runtime validator 明确针对 eager 的完整返回语义；M3 改变 end disposition 时必须同步扩展。
- 浏览器仅 smoke；未检查所有 viewport/浏览器、长时动画流畅度。临时 localhost 服务仅用于本轮验收。

## 下一项可执行任务

遵循 M0→M6 顺序进入 M3：先定义 loaded-state candidate 与显式退出/新站点卸载契约，扩展 compiler/runtime validator/recovery，测试整组 incidental 原子的合法最终承载。与正在进行的 row_column backend 任务协调接口与 schema，保持 rigid M1/M2 回归。

## 可复现环境

Windows / PowerShell，Python 3.12.1，Node v24.19.0；seed=0；硬件源 `configs/hardware/milestone1.json`，初始布局由 M1/M2 factory 提供。工作区所有项目文件起初均未跟踪，本轮未创建 commit 或发布。产物在 gitignore 下，按上述命令重建。
