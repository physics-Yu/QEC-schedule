# 当前交接状态

更新：2026-09-11。**M3-A–F 与原 M4 已在声明的有限平台/线路族完成**。M4主验收为单台AOD、单trap，checkpoint **schema17**、viewer **neutral-atom-view/2**保持。最新：[M4完整日志](logs/2026-09-11-m4-completion.md)、[策略合同](../docs/milestone4_complete.md)、[同平台比较](../docs/m4_acceptance.md)、[CZ方向调研](../docs/m4_physics_research.md)。M5/M6未实现。

## 最新：四逻辑 surface-code GHZ 同布局实验已完成

- [实验合同](../docs/surface_ghz_experiment.md)、[量子定义/一手资料](../docs/surface_ghz_research.md)、[最新完整日志](logs/2026-09-11-surface-ghz-experiment.md)。4块旋转[[9,1,3]]，36数据原子，从|0>编码并制备逻辑GHZ，194门=135H+59CZ；独立有符号稳定子+码距+实际效果顺序核验。无测量纠错/噪声/容错声明。
- 相同row物理映射10μm间距、5μm候选格点、单台1×36 rigid AOD及完整归还：row_symmetric **36083.7μs**；row_greedy **34956.7μs**，节省1127μs/**3.123%**。两份194/194、独立物理replay verified。源码运行中未变化，除compiler字段外输入完全一致。
- 先联合预置EZ、逐CZ恢复稀疏行、最后联合归还。greedy考虑全部READY/两分工/四方向，用同通道图去障碍最短距离作下界，实际构造59个CZ候选、剪枝4013、预算遗漏0。仍是受限恢复服务族，不是全线路最优或批量CZ。
- 工作台已开放128原子、4096门/列、1×1–1×128输入容量及可配置高预算/时限；1×9/1×36联合运输、满载恢复、本例36列完整物理执行有证据。大容量几何派生缓存和EZ空间筛选保持所有校验，schema17不变。
- 可编辑已完成结果 **http://127.0.0.1:8769/?job=6cc94f7ae83c46258f9056a66fe2bd54**，PID5880；新示例 **http://127.0.0.1:8769/?example=surface-ghz**；完整实验/线路/两动画 **http://127.0.0.1:8780/**，PID24816。旧8768/8767/8766未改。job深链接受内存8任务保留/服务重启影响，离线产物`artifacts/surface-ghz`永久在本地。
- 实际headless Edge完成示例载入→加H→撤销→点击编译194门→联合运输查看→已完成job深链接；无page error。两模式32×整段播放约43.06/44.73秒，均到真实终态且无停滞。CUA浏览器连接仍失败，这轮浏览器证据来自独立Playwright Edge，不是用户当前标签页鼠标操作。
- 本轮适用专项通过；主线程最终row6、量子6；子agent工作台/API/大输入及真实HTTP深链接、largeAOD、cache/EZ/CZ、A*/rowcolumn/恢复专项均通过，详见日志，**未重跑全量suite**。编译墙钟340.98/292.23秒是并发本机工作负载下单次实测，不当严格统计加速比或90秒保证。
- 成本：空载3790→2720μs（-28.23%），另增加57μs Raman重叠；载原子19988μs、装卸12200μs均不变。旧greedy高预算60秒探针仍在首CZ候选枚举；是主动限时诊断，非无解证明。
- 下一明确优化点：[排序分析](../docs/surface_ghz_ordering_analysis.md)。去掉各门最终必付的内禀成本后，离线几何模型预计空载1730μs；若继续须先接策略并完整物理/量子/回放重测，**不能将此预测列为本轮成绩**。加深有限beam在模型里反而不如简单校正，不要只放大搜索预算。

## 最新：M4 编译算法升级

**已完成的后续硬规则**：用户新增EZ四邻格禁止占据，并澄清允许穿过。共享后端、状态、交接及CZ后预测DAG已接入；38项规则、4项预测、最终95项M4/路径/cache/API回归、52项TaskIR/parking/planner及76项M1/M2/M3通过，无遗留失败。原12门greedy3824.5μs、lookahead3787.5μs均完整编译/独立重放；本轮未重跑全部460项。原8768下一次编译使用新规则，旧违反规则的轨迹需重编译。[规则合同](../docs/ez_neighbor_reservation.md)、[最新日志](logs/2026-09-11-ez-neighbor-reservation.md)。以下编译性能数字是加此硬规则前的历史结果，不能当新版本延迟保证。

本次接续用户 f902 十二门超时，已完成本轮定位/升级/验收，见 [升级日志](logs/2026-09-11-m4-compiler-upgrade.md)；不要把下方历史矩阵当作新 A* 的重跑证据。

- M4 单/多 trap rigid 已改为带 backend 扫掠的半格通道图 A*，明确≤2.5 μm 接入，完整容量边界和交接停点；固定图内最短，先验证几何下界路线以精确早停。[实现合同](../docs/astar_routes.md)、[调研](../docs/m4_pathfinding_research.md)。
- 重复验证优化：32项独立期望前缀缓存、精确只读 trace 解析缓存、可信内部持久状态 fork。每次仍核验 trace/runtime/DAG/pending，外部恢复审计保留。热缓存损坏和分支隔离 28 passed。
- 同输入前瞻编译124.427→55.967秒，完整模拟3787.5μs不变；贪心29.296→13.334秒，完整模拟3884.5μs不变。四份均12/12与完整归还，独立replay verified。新增交接停点改变分段/commits，不是逐字相同的轨迹。
- A*/M4/多trap/正交专项47passed；策略20passed；升级后0.25–32×两种回放映射Node验证通过。原8768服务与草稿未改，新worker加载当前代码；本次浏览器自动化连接失败，只核验HTTP200与回放控制，不声称实际鼠标浏览器验收。
- 最终全量 **460 passed /1 dateutil弃用警告 /731.26秒**。完整输出与前后profile/输入SHA256/性能摘要在 `artifacts/m4-timeout-f902/`。重复transition111190→16032；校验调用次数增加但重复工作减少。
- 阅读用户说明：[编译器升级报告](../docs/m4_compiler_upgrade.md)。未修改90秒上限、硬件参数或搜索预算；较大电路仍无延迟/成功率保证。

## M4 当前能力

- 同动作空间的 `basic / greedy / critical_path / lookahead`，可在原工作台编辑任意受支持门后重新编译；初态与完整终态相同。旧M3 resident/returning/legacy保留为历史对照。
- CP按全部READY门的剩余脉冲关键路径排序；lookahead在私有状态真实执行有界服务，比较KEEP/RETURN、实际终态清理和剩余脉冲下界，记录next-use/驻留特征。有限视野评分不是整个未来电路的精确成本或全局下界。
- MOVE解析安全Raman子窗口；不再因为整个移动曾靠近就放弃安全早段/后段。原独立脉冲轨迹校验、静止AOD锁、支撑与资源约束保持。
- 修复预算恰好用尽被误标截断、全部分支失败丢原始原因、EZ日志在错误末态重验并行Raman。故障注入确认失败不修改live state。决策详情包含实际深度、节点与遗漏、评分及编译时间。
- 一台rigid AOD输入支持1×1–1×128，10μm多列固定间距、按需开关、联合装卸/预取/终态归还；本轮新增1×9/1×36实测。四M4策略及新稀疏行策略可调用，多格点需adaptive EZ。之后仍主要cell0门服务，尚无任意多cell跨门驻留/部分交接优化或批量CZ。

## 保留的物理与产品规则

- EZ SLM上还有未完成CZ的原子，为其唯一下一次CZ伙伴保留上下左右一个网格间距的四个EZ点；无关原子允许经过，但不准SLM/AOD停驻或卸载占据。保护按holder/DAG派生，CZ完成切换或解除；非可选项，不是整圆禁区，旧轨迹需重新按新规则审核。

- 门集H/X/Y/Z/T/CZ，单比特固定1μs；只同类型门可并行，CZ仍逐门执行。可与无冲突运输重叠。
- Raman目标可在稳定SLM或静止AOD，完成交接、存活未测量、位于寻址区；与任意其他存活原子距离≥5μm（含5），<5禁止；空trap不是原子邻居。
- CZ左右/上下相对位移2μm均保留，四方向同样检查碰撞、区域、支撑、静止和实际作用对。项目是全EZ照明与各向同性距离代理；没有真机方向保真度等价的证据。“中心上2/下2”是相距4μm，当前模型拒绝。
- M4仅横平竖直，主通道2.5+5kμm，短正交端点接入，显式LOAD/脱离/OFFLOAD和完整轨迹避碰。
- 固定pipeline：初始条件→可编辑线路→真实编译/独立验证→Executor→recorder/viewer；随机、撤销、导入导出、失败弹窗和32×保持。旧checkpoint1–16需从输入重编译。

## 此前 M4 验收与交付入口（算法升级前）

- 新交付 **http://127.0.0.1:8768/**，PID **9692**，`artifacts/workbench-m4-complete`，tab19已标记交付。旧8767 PID8364与8766 PID10880未停止，用户原草稿未刷新。复用端口前核验。
- 20组同平台运行与25份实际记录独立replay verified。重复pair：basic2856.9 vs greedy952.9μs；分支反例：basic2956.9、greedy3023.9、CP2398.9、lookahead2078.9μs，均含归还。完整矩阵/源码指纹/预算在验收文档。
- 全量pytest **417 passed / 1依赖弃用警告，1196.58s**。该轮导入后只追加了诊断修复，最终补丁后策略整模块 **20 passed / 37.53s**（含故障注入）；不声称物理矩阵和全量均由最后诊断版本重新运行。
- 主agent窗口+编辑器专项20passed；方向模型5passed；统一比较测试4passed。浏览器真实加H后CP5/5门983.6μs，随机8/8门1046.3μs，两种回放模式32×到终态。basic四H在1μs同时执行；预算1失败保留2/4门476.3μs，返回编辑后重编译成功。
- 最终页面恢复四门lookahead，952.6μs/28ops，停在CZ476.15μs；Node四份录制0.25–32×两种时间映射通过。仅界面观察，不是长时FPS或真机验收。
- 最后诊断补丁不改评分/候选/执行顺序。矩阵明确保留开始编译指纹和source_changed=true，最终诊断独立留证；generator导出的ready/site预算已与run_options逐份同步，物理记录未改。

## OPEN 边界与下一步

原M4在以上范围收口，不表示优化结束。本次A*只解决固定rigid通道图上的局部最短路；有限目标/开关候选/深度、非抢占单AOD服务，仍不能保证所有32原子/64门或任意障碍布局成功，也不保证全局最短/最大并行。旧版复杂lookahead曾CLI编译151–168秒；这些旧大例未作为本次升级性能重跑，不能从f902已降至90秒以内推导全部输入不再超时。失败须如实保留。

当前用户优先推进M4编译性能和路径规划；本次回归/交接已完成，后续依据剩余profile考虑重复几何构造与候选公共前缀，不能静默转入M5。原milestone的下一阶段M5仍需先明确batch CZ光照范围和actual/intended pair集合，再做多pair分配及旁观pair负例；不可把多trap联合运输叫作批量CZ。多AOD、真机标定、量子态/噪声/保真度与RL仍未实现。

历史：[多trap](logs/2026-09-11-multi-trap.md)、[AOD Raman](logs/2026-09-11-aod-raman.md)、[M3完成](logs/2026-09-11-m3-completion.md)。未创建Git提交或发布，代码变更仅在本地工作区。
