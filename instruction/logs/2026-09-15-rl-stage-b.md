# 2026-09-15 · 隔离 RL 第二步

- 状态：COMPLETED（小规模训练与完整反馈）；性能优势验收未通过。
- 目标：真实小规模学习、未训练/模仿/PPO与独立局部成本/随机对照、未见电路测试；不污染旧greedy与工作台。
- 本轮开始保留既有src哈希于artifacts/rl-stage-b/existing-source-sha256.json。
- 主解释器Torch2.2.2与NumPy2.5.3不兼容；用.venv-rl-stage-b（只读继承系统包，局部NumPy1.26.4覆盖）隔离，未修改系统依赖。
- 先保持阶段A物理候选空间。新精确状态LRU只缓存已验证候选，key包含完整checkpoint/终态/生成配置，提交仍由Executor验证。无近似轨迹替代。

## 完成内容与结果

- 详见 [合同/报告](../../docs/rl_stage_b.md)。新增10,594参数图actor/critic、NeuralPolicy加载、模仿＋PPO、gamma1 GAE、并行连续片段、精确缓存、拆分manifest、反例诊断与验收报告。
- 正式结果artifacts/rl-stage-b/attempt2-normalized-validation：13训练/4验证/9测试，两seed11/29各24更新，55教师决策、615 PPO决策、90片段续接、训练失败0，总耗时208.570秒含全评估/导出。
- 108/108测试执行、终态、逐门效果一次、独立重放通过。训练后反例诊断16次同样通过。已保存并重新加载selected.pt到独立自定义H/Y/CZ/Z电路：849.324143μs，重放/效果/终态通过。
- 主目标（平均负归一化回报）：未训练5.353074、模仿3.722943、PPO3.611832、local_cost3.611832、随机5.994845。PPO与local_cost整体打平；seed11选update0模仿，seed29选update16。不能只报后者或拿平均耗时比0.997222宣称训练主目标改善。
- 物理可实现的训练反例3μs仍用4μs，测试反例6μs仍7μs。performance_status=NOT_ESTABLISHED，未稳定超越基线。原因调查：教师含次优动作；小预算PPO未纠正，探索/片段价值/表示能力须后续消融，未证实单一原因。

## 失败与修正记录

- attempt1正常训练/物理执行，代码复核发现选模使用原始μs，而训练按样本类型归一化。该单位不一致使CZ权重过大；修正score并添加回归测试，保持同训练配置完整重跑attempt2，保留attempt1。
- 初始PyTorch/NumPy兼容问题仅在隔离venv处理。系统NumPy保持不变；venv继承的SciPy不兼容本地NumPy1.26，本实验不导入SciPy，不能将该venv用于全项目。
- 不调低物理校验，不改测试期望掩盖错误。测试集不用于梯度、选模或超参数调节；完成本轮诊断后，后续调参应另冻结新测试集。

## 验证

- `.venv-rl-stage-b/Scripts/python.exe -m pytest tests/test_rl_stage_b.py tests/test_rl_stage_a.py tests/test_environment_boundary.py -q`：最终37通过。
- `python tools/check_architecture.py`：153模块零违规。
- 哈希审计：本轮开始157个既有src文件全部未变，包括greedy、环境、应用与阶段A。
- 输出报告链接与文档相对链接检查；没有本轮真实浏览器交互验收，没有提交推送，没有修改8798入口。

## 下一步

新建独立训练/验证反例族，做模仿强度、探索和片段长度/价值误差的受控消融；冻结新测试集。先通过长程反例和跨seed稳定收益，再扩大长电路及驻留自由度。复现命令与输出字段见合同。
