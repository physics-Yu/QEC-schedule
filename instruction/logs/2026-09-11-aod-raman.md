# 2026-09-11 · 静止 AOD 单比特门与新可编辑交付

- 状态：IN_PROGRESS
- 用户目标：核对 AOD 承载目标可否打单比特光；有文献支持则实现并交付可编辑、可真实重编译的可视化。
- 起点：上一轮 CZ 四侧及邻距修正 PARTIAL，schema 16；此前已验收门契约 schema 15。本文接续完成，不将历史成绩计入本轮。
- 规范：[AOD Raman](../../docs/aod_raman.md)、[门规范](../../docs/gate_contract.md)、physics / motion_execution / visualization。

## 内容与依据

核对 R2 作者预印本 Methods：支持 AOD/SLM 上局部单比特门。开放静止 AOD，保留当前有效 zone、开启支撑、存活未测量、交接完成资格。≥5 μm / 1 μs 为用户模型参数，论文未证明该组合实验保真度。

`hardware/raman.py` 校验目标和全脉冲区间内邻距；`motion/task_validation.py` 与 `validation.py` 为 AOD 目标锁运输资源；`greedy.py` 提供原位、分离和备用安置候选，CZ 比较四侧。`recording.py` 记录实际 target_holders，viewer 显示双 T 各自承载；工作台新预设和配置可编辑。schema 17 拒绝旧 checkpoint。包含上轮尚未整体验收的四侧与严格 <5 禁光修正。

## 当前证据

- 首次原专项 40 pass / 1 fail：唯一失败是旧测试预期所有 AOD 目标被拒绝；已按新契约更新。不能将此失败隐去或作为最终成绩。
- 新 AOD/旧 Raman 专项：26 passed，8.47 s，`artifacts/aod-focused.log`。
- 五份矩阵独立 replay verified：AOD 双 T 复用 985.6 μs；四 H 1 μs；H/X 分组 2 μs；shuffled seed 13 为 1065.6 μs；预算失败保留一门与 476.3 μs 片段。`artifacts/aod-acceptance.log`。
- AOD 双 T 例两次 CZ 之间没有 LOAD/OFFLOAD；两个 T 同时 1 μs 且 holder 分别 static/mobile。
- 四份 HTML 的关键帧 1×/32×、暂停续播到终态通过：`artifacts/aod-playback.log`。
- 浏览器首次验收发现新 caption 使用了 draw 局部变量 scene，导致侧栏更新中断；已改 data.scene 并重新生成 HTML，后续复验待记录。
- 完整 pytest、HTTP 编辑和最终浏览器复验进行中；不能使用之前 340 passed 作为本轮结果。

## 服务与限制

8767 PID 18388，产物 `artifacts/workbench-aod-raman`；8766 PID 1308。PID 复用前核实。未刷新用户原标签页草稿，另开最终验收 tab 16。

保守完整 MOVE 窗口、有限候选、单 AOD 非抢占限制见规范。未进行光场/保真度或长时 FPS 验证。未提交 Git 或发布。

## 下一步

完成本轮回归、HTTP 和真实浏览器编辑/32×复验后更新为 COMPLETED，并更新 handoff。后续 M4 继续 next-use / lookahead，以相同平台、终态和物理约束比较。
