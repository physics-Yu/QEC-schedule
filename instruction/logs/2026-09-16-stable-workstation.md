# 2026-09-16 工作台版本整理与详细编译说明

## 需求与根因
用户指出常用算法重复、描述不清、受限旧实现混入，初始偏移重要性不明确，配置页面挤成窄列。读取实际有序贪心/SMT/controller与配置后，按当前完整接入能力收敛菜单；没有通过改名字将旧算法升级为新算法。原CSS的max-width950px `.settings`双列与两张并排卡片叠加，是截图850–950px挤压的明确原因。

## 改动
- configs/studio/workbench.json 当前只列ordered_greedy、smt_ordered；移除六个旧受限策略的常用选项。各自维护版本、用途、实际选择方式、取舍及验证范围；加载器要求说明齐全、拒绝旧实现进入当前目录。
- 当前后端正交有序行列、路线axis-hold只读展示；旧实现保留代码/API，不静默修改旧配置。旧自定义草稿须点击更新按钮；保留原子数、布局、门、行列数和偏移，重置为当前调度默认预算并标记旧结果过期。专用demo保持其验证合同。
- 界面分离平台与完整编译流程；初始偏移折叠，增加10μm初态恢复，说明初始/归还成本和边界作用。实时轴规划能力保持。
- 贪心/SMT预算分别显示，无读出线路隐藏测量配置；修复锁定demo的最大决策字段可编辑外观。
- 配置卡内部单列、宽字段；1100px以下卡片上下排列；旧高级控件仅在隐藏兼容结构中保留。详细能力/限制/配置归属见docs/workstation_compilers.md。
- 编译核心与物理硬约束未改；RL不纳入。

## 验证与失败记录
1. 核心组合pytest：test_studio_config、test_studio_config_files、test_ordered_workbench、test_dynamic_aod_workbench、test_workbench_compilation_config共70通过。
2. 后续恢复六个旧API兼容参数回归，并增补目录排除旧版/缺少说明及方法预算分离断言；studio两文件31通过，合计覆盖79个不同用例。初次测试仍把returning设为目录默认导致失败，按照新目录合同改为SMT；子进程编码同时显式UTF-8，避免Windows GBK读中文错误。没有改动物理断言。
3. 实际编辑器JS+HTTP：普通原子/布局/非均匀轴、贪心和SMT真实编译、菜单淘汰旧项、旧输入导入后显式升级且参数保留、恢复偏移不改线路、demo/自定义切换通过。DOM/viewer替身不等于浏览器GUI。
4. 4×6 AOD、6个row原子、3CZ同批实际执行完成；物理1362.933739μs，本次编译2.437s。不是隔离性能基准或所有4×6电路覆盖证明。
5. 架构检查161模块0违规（包含本地RL）；Demo132文件哈希/链接和10项HTTP通过；唯一HTML ID和JS语法检查通过。
6. CUA getState仍报nodeRepl.fetch request failed，真实屏幕排版/触摸/缩放未验收。不以静态CSS或DOM替身宣称GUI PASS。

## 交付与限制
工作台 http://127.0.0.1:50652/?job=83c81f63376c4c05ad4d80478d444bab ，PID34060。
artifacts/stable-workstation/jobs/83c81f63376c4c05ad4d80478d444bab 包含实际输入、动画、checkpoint、decisions、统计。旧49870、63791与历史研究端口保留；旧Python进程的目录快照不会自动刷新，用户应从新入口进入。
仍是有界候选、每批归还/完整服务串行；不声称已解决全局驻留、完整并发或任意大电路。下一步为浏览器连接恢复后检查配置页在窄窗口与桌面窗口的真实布局。
