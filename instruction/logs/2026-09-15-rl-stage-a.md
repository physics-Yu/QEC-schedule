# 2026-09-15 · 隔离的 RL 阶段 A

- 状态：COMPLETED（阶段A基础接口；不代表RL训练完成）
- 用户要求：实现第一步，与旧 greedy 隔离，长电路未来采用连续分段训练。
- 方案：仅新增 learning / experiments/rl / configs/rl，复用物理环境及无状态路线工具，不注册到生产策略/工作台。
- 开始时已保存所有既有 src 文件 SHA256：artifacts/rl-stage-a/existing-source-sha256.json。
- 先验收随机/独立局部成本策略、小电路、奖励、片段续接、失败和吞吐；没有训练权重。

## 完成与验证

- 新模块及合同见 [RL阶段A](../../docs/rl_stage_a.md)，没有更改生产registry、greedy、环境物理或8798工作台。
- `python -m pytest tests/test_rl_stage_a.py tests/test_environment_boundary.py -q` 最终29通过。首次26过1败：恢复观察包含非确定的planning_seconds；已将墙钟计时移到transition诊断，保留观察严格相等断言，复验27通过，新增RNG/reset/异种门测试后最终29通过。
- `python tools/check_architecture.py` 146模块零违规。
- `python -m neutral_atom_experiments.rl.evaluate --workers 2 --output artifacts/rl-stage-a/acceptance-final`：24/24完成、门效果恰好一次、终态、重放全部通过；96.128秒。首次attempt1同样24通过，保留旧观察诊断格式证据。
- 8原子局部成本策略：4对CZ并行，847.324143μs；random seed0为1771.264138μs。两者同输入哈希，均真实执行/重放；无学习效果声明。
- 既有150个源码文件逐字哈希不变：artifacts/rl-stage-a/isolation-audit.json。代码隔离测试同时约束新模块不依赖生产scheduler、旧模块不导入learning。
- 端到端验收吞吐1.228决策/秒，含重放/导出；规划占规划+执行累计时间90.85%。没有测受控并行加速比，没有训练网络。

## 限制及下一步

- 有限恢复式CZ、同种1Q选择；未支持测量、跨批驻留和操作内部并发。片段支持连续状态与bootstrap标志，但尚无GAE/价值网络。
- 本轮无真实GUI交互验收；交付为独立静态报告及共用viewer真实程序动画，不替换原工作台。
- 下一步审计候选覆盖与构造开销，在独立包内接小规模学习；不要为加速跳过物理校验。未提交推送。
