# 2026-09-11 · M3-B 独立任务与操作 IR

- 状态：COMPLETED（M3-B 串行 IR 实现/机器验收完成；真实浏览器连接失败，M3 整体未完成）
- 用户目标：继续执行 M3；从交接指定的 M3-B 独立目标任务/操作 IR 开始。
- 范围：零 gate 运输、prepare/effect/cleanup 拆分、目标与约束、逐操作依赖/资源/时间、唯一门效果、checkpoint 与共用观察端。保持串行；不提前声明 M3-C/D/E/F 完成。
- 约束：固定 Raman 1 μs、最高 32×、初始条件优先和既有工作台 pipeline；不放宽物理校验、不修改初态掩盖失败。

## 实施内容

- `domain/operations.py`：TaskIntent、TaskTarget、OperationInterval，显式终态及原子/站点/时长约束，操作 gate_id 与串行 depends_on；CompiledPlan 保存区间和原始 DAG。
- `motion/tasks.py`：TargetTaskCompiler 支持零 gate SLM→SLM、暗阱定位、原位 CZ/U3；TaskProgram 延迟到最新状态编译，split_gate_program 在完整 pulse 前后拆分，不截断交接豁免。
- `motion/task_validation.py`、program/validation：独立 replay 核对目标、真实资源/原子、依赖、成本、一次门效果、初态指纹与重复 task_id；目标和候选失败不提交状态。TaskCompiler 协议与 GateCompiler 并存。
- physical_executor/runtime_validation：gateless 任务不预约或改变 DAG；effect 阶段提交唯一门效果并释放后继；记录 task_id/phase/related_gate_id/operation_ref；保留单 active_plan、全计划预约。legacy CZ 也补 effect_completed 追溯标记。
- state/checkpoint/operation_codec：schema **12** 完整保存新 IR；旧 1–11 显式拒绝。验证任务全部操作边界的恢复，检查原始 DAG、trace/cursor、预约、holder/axes/masks/transfer 和门效果一致。
- recording/summary/viewer：支持空 gate 元数据和任务标签、任务起点已经 loaded 的路径；cleanup 归入回程，trace 与 recorder 汇总一致，复用 /2 与现有 UI。
- `examples/run_task_program.py` 使用既有工作台 JSON/build_inputs 与共用 recorder/viewer，输出独立任务演示或零门运输；保存 input/plans/recording/checkpoint/diagnostics/result/index，stalled 非零退出且保留部分结果。
- 新增 [任务 API 与边界](../../docs/task_program.md)，同步 agent、首页、当前 handoff、milestones、compiler_contract、state_circuit、architecture、model_audit、相关运动/可视化规范与索引。历史已完成日志和前次有日期总览保持原样。

## 验证结果

| 命令 / 检查 | 本轮结果 | 范围 |
| --- | --- | --- |
| `python -m pytest tests/test_raman.py tests/test_dynamic_traps.py -q --tb=short` | **36 passed，21.54 s** | 保留固定 Raman 成本、混合执行、动态 masks/交接/扫掠与恢复 |
| `python -m pytest tests/test_task_ir.py -q --tb=short` | **10 passed，36.23 s** | 无门真实运输、拆分成本/逻辑/唯一效果、loaded 边界上的稳定 SLM Raman 串行插入、约束和损坏恢复 |
| `python -m pytest -q --tb=short` | **263 passed，1 warning，428.41 s** | 完整机器回归；启动后补了 trace/recorder 汇总一致性断言并调整 viewer/summary，相关最终版本以下面的复验和 Node 为证，不把新断言冒称已在这次全套收集 |
| `python -m pytest tests/test_task_ir.py tests/test_visualization.py -q --tb=short` | **16 passed，72.50 s** | 包括新增 trace/recorder 类别一致性与最终任务行为；与完整回归有运行时间重叠 |
| `python examples/run_task_program.py` | completed | 6 原子、shuffled seed 7、4 门、6 任务、26 operations、64 commits，见下表 |
| `python examples/run_task_program.py --transport-only --output artifacts/m3-task-transport` | completed（仅运输） | 输入 DAG 保留，Q000→EZ0，6 operations、14 commits，完成门数 0 |
| `node tests/task_controls.cjs` | PASS | 实际两份记录、gateless 不画门、任务标签/区间、一次效果、cleanup、数据不变 |
| `node tests/raman_controls.cjs artifacts/m3-task-program/index.html` | PASS | 实际 3 个固定 1 μs 原位 Raman、参数与分类、完成状态 |
| `node tests/viewer_speeds.cjs artifacts/m3-task-program/index.html` | PASS | 两种时间模式 0.25–32×、结束停止、物理数据不变 |
| 两份最终 checkpoint restore/snapshot | PASS | schema 12 完成态逐字节 round-trip |
| 文档相对链接目标与代码围栏 | PASS | 当前改动文档的本地引用均存在；历史快照不改写 |
| CUA 浏览器 | 未通过连接阶段 | getState 返回 browsers=[]，nodeRepl.fetch request failed；未进行本轮真实浏览器视觉验收 |

warning 为既有 dateutil `utcfromtimestamp` 弃用提示。Node 首轮发现新增 Raman 任务提示提前引用 draw 内 `op` 的初始化顺序错误，改为按当前时刻取操作，再从源模块重建 HTML；三个 Node 检查均重新通过。没有用离线 DOM/Canvas 替身冒充浏览器 FPS 或人工视觉 PASS。

## 实际成本与复现产物

| 场景 | wall μs | Raman / CZ μs | AOD / 原子路程 μm | LOAD / OFFLOAD |
| --- | --- | --- | --- | --- |
| 混合 4 门拆分为 6 任务 | 1163.2898987322333 | 3 / 0.3 | 279.99494936611666 / 181 | 3 / 3 |
| Q000→EZ0 独立运输 | 338.2842712474619 | 0 / 0 | 69.14213562373095 / 55 | 1 / 1 |

标准输入为 `configs/workbench/mixed.json`，和先前未拆分工作台的物理成本相同；拆分额外产生两个 plan 的开始/完成，因此 commits 从 60 变为 64。物理 gate 和 operation 没有增加，不靠隐含零成本准备满足起态。

产物 `artifacts/m3-task-program/` 与 `artifacts/m3-task-transport/` 可用上述 CLI 重建；均保存计划 IR、输入、结果、诊断、schema 12 checkpoint 和共用 HTML/JSON 回放。生成后 viewer 提示修复使用 `visualization.viewer.write_html` 从 recording 重新构建，没有手改生成 HTML。

## 决策与限制

- M3-B 当前目标域为 exact holder 子集 / 完整 axes / 完整 masks 的合取，资源约束为允许原子/SLM 站点和总时长预算；串行链给出相对操作时序。通用允许终态集合/区域 allocator 和排程时间窗口不在这次实现中。
- TargetTaskCompiler 的路径构造限单 rigid trap、一个 SLM 原子的显式目标及空阱定位，最多 64 个载原子路径候选。TaskProgram 可验证 loaded 中间起态，但不能据此声称任意 loaded 目标可编译。
- 在 CZ effect 和 cleanup 之间插入另一稳定 SLM 1Q 的测试是串行续接，不是 1Q/运输重叠。分操作资源区间是 IR，实时预约仍整计划，M3-D 未完成。
- 默认工作台仍沿用兼容 eager 门循环；任务演示读取同一输入，走同一物理与观察管线。本轮没有新增参数选项，没有改变 1 μs、32× 或随机输入约定，没有刷新用户草稿或重启 8766。
- schema 12 拒绝旧 1–11，从原始输入重编译；不能改旧 trace 恢复。单任务 checkpoint 恢复当前已提交任务，跨任务未来策略/通用 loaded 续接仍由后续调度负责。
- 本地 Git 仍全部未跟踪，本轮无提交、发布或对外消息。历史远端记录不代表本次源码已同步。

## 状态与下一步

M3-B 串行任务/参数化 1Q IR 已验收。GAP-003 只标部分修复，保留通用 loaded 编译/KEEP 续接和终态分配缺口；GAP-005–009 仍 OPEN。M3-C/D/E/F 尚未完成，未改变原 rigid-parking 对角门失败负例。

下一项为 **M3-C 单 trap 持久状态编译与退出**：读 milestones、compiler_contract、motion_planning，基于 TaskIntent/TaskTarget 定义可支持平台族，实现 EZ SLM/loaded/异位空 AOD 起点、KEEP 后下一门、RETURN_ONLY、新站点卸载和退出；全部附带原子都可安置，保持零门 DAG 不变、唯一效果、目标/资源校验、恢复和动态支撑负例。之后再做 M3-D 最小操作重叠。
