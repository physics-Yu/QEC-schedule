# 2026-09-10 · 架构拆分与下一阶段交接

- 状态：COMPLETED（架构拆分、定向文献核对、代码审计和交接校验完成；运行时缺陷保持 OPEN）。
- 用户目标：减少每次阅读原架构的负担，形成行为入口、按需 instruction、文献支持的物理解释与稳定交接日志。
- 本轮范围：文档重构、文献核对、只读代码审计、现有基准复核；未开始 M2、未修复 runtime 缺陷。
- 开始基线：M0 基础及 M1 单门受限流程；用户已认可当前视觉风格。

## 1. 本轮完成内容

1. `agent.md` 作为精简入口：行为准则、当前框架、选读路由和交接义务；`AGENTS.md` 只作短入口，不维护重复正文。
2. 原 architecture 全文按字节归档，根路径替换为短导航，`migration.md` 对原 89 章逐一映射并说明旧要求的替代关系。
3. 新增 architecture/state_circuit/physics/motion_execution/planning_rl/milestones/validation，保留并连接既有 visualization/aam 和 M1 实例。
4. 新增 research：核对 6 篇研究作者论文及 2 份本地来源；区分物理事实、项目简化、能力限制和需标定参数。
5. 新增 model_audit：复现两个逻辑/恢复缺口，记录多周期指标、参数门与 grid 元数据问题，避免下一位 agent 重复踩坑。
6. 新增 workflow、handoff 和日志索引，约定开始/结束工作时的最小读取与更新路径。

## 2. 既有工作的回顾性汇总

以下来自当前代码、产物及本会话记录，不伪造早期逐日开发日志：

- M0：状态/holder/DAG/事件/快照基础；原子与物理比特统一 Q；三区按用户方向排列；机器断言与代表性场景分离。
- M1：只运输 Q000 到非 SLM 作用点，与预置静态 Q001 配对；刚性 spacing 和 2 μm 阈值固定；含附带原子与失败诊断。
- UI：独立生成模板；等尺寸 SLM/AOD trap 圆环、不同承载 marker、活动状态色、编号和图层、详情与播放控制。
- 先前的浏览器自动打开被安全策略阻止；已有离线控制和静态关键帧证据，未据此声称浏览器验收通过。

## 3. 本轮验证

| 命令/检查 | 结果 | 说明 |
| --- | --- | --- |
| `python -m pytest -q` | 80 passed，1 warning，10.29 s | 既有 dateutil 弃用警告；不覆盖本次新发现的两个缺口 |
| `python examples/run_single_gate.py` | 5 场景 PASS | baseline/incidental 成功；unintended/blocked/both_storage 按预期拒绝 |
| [审计脚本](2026-09-10-audit.md) | 2 个缺口已复现；多周期口径已核对 | 内存状态，不改配置 |
| 原 architecture 归档 | SHA-256 一致 | 原文 56,334 bytes，全文保留 |
| 文档链接/锚点、原 89 章、源码指纹 | 全部通过 | 23 份文档，211 处相对链接（含 1 处锚点），89/89 章映射，3 份原件哈希一致，51 个运行时文件未变 |

基准关键数字：pulse 156.3 μs；总周期 312.3 μs；AOD 56 μm；baseline 原子路程 56 μm、incidental 112 μm。两次同 pair 周期得到仿真时间 624.6 μs，仅证明该受限管线可重复，不是 M2 全面验收。

## 4. 文献与工程判断

具体证据和链接集中于 [research](../research.md)，避免多份文档重复长篇摘录。本轮采用原始研究，不依赖搜索摘要中的性能倍率作为本项目验收目标。

- 连续 AOD pose 与静态 trap 不是同一个格点限制。
- 刚性只允许整体平移，不允许私自缩短同阵列原子间距。
- 光势、加热、相位和散射未被当前几何模型覆盖；这些被明确列为有限适用的抽象，不要求在 M1 补成完整量子模拟器。
- 逻辑事件绕过物理检查、损坏活动队列仍恢复是内部一致性问题，不能归为合理简化。
- 不改变 M0–M6 路线；把原架构中提前要求 KEEP/多门图的内容归回后续阶段。

## 5. 剩余工作

BUG-001/002、EXT-001/002、META-001 保持 OPEN；细节见 [model_audit](../model_audit.md)。下一开发任务先封入口/恢复缺口，再按 M2 连续 eager 调度推进。真实浏览器自动验收仍未完成。

## 6. 可复现与文件归档

- 工作区：`C:/Users/86136/Documents/ChatGPT/QEC-scheduler`；PowerShell；本轮命令使用本机 Python 3.12。
- 原 architecture SHA-256：`9862cc2c58f0aba79b255159b8f86b19e5e69e45fd662dda48df63574eb8d869`。
- AAM 原件 SHA-256：`311fadeffee8c0b86bb91cb0c1ddb8e337edfdb3cdd092e62f730ab6088e9c83`。
- ISA PDF SHA-256：`336d37cb5bee737f2ebbdf6d0ebdf895ff4f4643de899d378045e9e7aa1f1391`。
- 审计前运行时代码/配置/测试/示例共 51 个文件的指纹存于 `artifacts/handoff/source_manifest_before.json`；最终比较用于确认本轮不改物理程序。
- 最终检查摘要：`artifacts/handoff/documentation_check.json`。原架构 3,813 行；常驻 `agent.md` 61 行，`handoff.md` 51 行，其余按需选择，不默认读全文。
- 不创建新任务，不提交或发布；下一位 agent 先读 [当前交接摘要](../handoff.md)。
