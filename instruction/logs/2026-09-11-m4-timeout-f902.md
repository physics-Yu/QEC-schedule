# 2026-09-11 · 当前十二门线路的90秒超时核查

- 状态：COMPLETED（超时定位）；后续算法升级另见同日升级日志。
- 用户问题：任务 `f902f3305086474a89146d091f9c5035` 为何在11/12门时超时，是否最后AOD trap不能编译。
- 输入：7原子、row/seed7、单AOD 1×2、lookahead、beam2、rollout6、site1、12门。原始输入和失败报告保留在 `artifacts/workbench-m4-complete/f902f3305086474a89146d091f9c5035/`。
- 原记录只能证明worker被90秒deadline终止，11/12是最后上报进度；不能据此断言物理无解。

新增 `examples/diagnose_workbench.py`：同输入、预算和物理规则离线复现，记录候选生成/策略推演/真实执行进度耗时，保存实际结果与可复用回放。只包装日志，不改候选或验证，不修改用户当前浏览器草稿与服务器时限。

复现命令：

```powershell
python examples/diagnose_workbench.py --input artifacts/workbench-m4-complete/f902f3305086474a89146d091f9c5035/input.json --output artifacts/m4-timeout-f902
```

原输入离线完整成功：12/12 门、完整终态归还，124.427 秒，3787.5 μs、116 operations、8 plans、10 LOAD/10 OFFLOAD。到原失败完全相同的 142 commits / 11 gates / 1819.2 μs 时，剩余门是 G009 CZ(Q000,Q006)，不是线路最后一列 G011；G011 已完成。G009 有 8 个合法候选、0 拒绝，生成用时 0.542 秒，但随后前瞻比较用时 29.31 秒，将主机时间从 87.065 秒推到 116.375 秒。HTTP 90 秒在这期间终止 worker，不能解释成 AOD trap 无法执行。

仅将策略换为 greedy 的对照：29.296 秒，12/12，完整归还 3884.5 μs、120 operations、7 plans、11 LOAD/11 OFFLOAD。原前瞻慢约 4.25 倍，只节省 97 μs（约 2.5%）总模拟时间。

两份结果分别保存在 `artifacts/m4-timeout-f902/`、其 `greedy/` 下，均经 `python examples/verify_m3.py <directory>` 独立 verified。原 lookahead 的 `node tests/viewer_speeds.cjs <directory>/index.html` 覆盖 0.25–32× 两种时间映射通过。原用户草稿、服务和 90 秒上限未修改。

用户进一步要求研究最短路径和编译算法，本问题转入 [编译算法升级](2026-09-11-m4-compiler-upgrade.md)，不以提高超时上限作为修复。
